source [find interface/stlink.cfg]
transport select swd

# Target MCU (Cortex-M3 STM32F1)
source [find target/stm32f1x.cfg]

# Reset configuration
reset_config none

# Adapter speed (1 MHz safe default)
adapter speed 1000

init
reset halt